{
  "github": {
    "silent": true
  },
  "buildCommand": "cd ../.. && npm install --force && npx browserslist@latest --update-db && npm run build",
  "outputDirectory": "apps/nextjs/.next",
  "installCommand": "echo 'Using buildCommand for installation'",
  "framework": "nextjs"
}
