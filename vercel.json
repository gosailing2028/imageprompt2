{
  "github": {
    "silent": true
  },
  "buildCommand": "npm install --force && npx browserslist@latest --update-db && npm run build",
  "outputDirectory": "apps/nextjs/.next",
  "installCommand": "echo 'Skip install, using buildCommand'",
  "framework": "nextjs"
}