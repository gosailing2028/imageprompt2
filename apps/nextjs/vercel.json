{
  "github": {
    "silent": true
  },
  "buildCommand": "npx browserslist@latest --update-db && npm run build",
  "outputDirectory": ".next",
  "installCommand": "cp package-deploy.json package.json && npm install",
  "framework": "nextjs"
}